name: dublin-bus-rtpi
channels:
  - defaults
dependencies:
  - python=3.7
  - numpy
  - pip
  - pip:
    - django
    - pyleapcard
    - jupytext
    - pymysql
    - sshtunnel
  - pandas
  - scikit-learn
prefix: /home/student/anaconda3/envs/dublin-bus-rtpi

